{
  "name": "imr-test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "rm -rf dist && parcel src/index.html",
    "build": "rm -rf dist && parcel build src/index.html --public-url /imr-test/dist/"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-static-files-copy": "^2.3.1",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "preact": "^10.3.3"
  }
}
